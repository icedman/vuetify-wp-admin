const domain = 'http://wordpress.localhost'

export default {
  wordpress: {
    domain: domain,
    api_entry: `${domain}/wp-json`,
    ajax_entry: `${domain}`,
    wc_consumer: {
      key: 'ck_XXXXX',
      secret: 'cs_XXXXX'
    }
  }
}
